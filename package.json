{
  "name": "@txstate-mws/fastify-shared",
  "version": "1.1.9",
  "description": "Types and other code shared between a fastify-txstate API and the UIs it communicates with.",
  "exports": {
    ".": {
      "types": "./lib-esm/index.d.ts",
      "require": "./lib/index.js",
      "import": "./lib-esm/index.js"
    }
  },
  "types": "./lib-esm/index.d.ts",
  "devDependencies": {
    "@types/node": "^24.0.0",
    "json-schema-to-typescript": "^13.1.2",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3"
  },
  "scripts": {
    "prepublishOnly": "npm run build",
    "build": "rm -rf lib lib-esm && tsc && tsc -p tsconfig.esm.json && tsx build.mts && cp package-esm.json lib-esm/package.json"
  },
  "files": [
    "lib/**/*",
    "lib-esm/**/*"
  ],
  "author": "Nick Wing",
  "license": "MIT"
}
